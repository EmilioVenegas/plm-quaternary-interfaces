# environments (two are required; see bin/setup.sh -- `esm` pulls its own torch and
# must not share an env with the pinned core pipeline)
.venv/
.venv-esmc/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# bulk downloads: regenerable by scripts/fetch_data.py, never committed
data/proteingym/bulk/
data/proteingym/*.zip
data/megascale/
data/structures/*.pdb

# cached model weights
*.safetensors
*.bin

# agent instructions and internal notes (never committed)
AGENTS.md
CLAUDE.md
*HANDOFF*
*TODO*
*NOTES*
.omp/
.claude/


# planning and masterplan documents (untracked)
MASTERPLAN.md
*PLAN*
*PRE_REGISTRATION*
docs/discovery_axes.md
# build / packaging artifacts
*.egg-info/
build/
dist/
vendor/
